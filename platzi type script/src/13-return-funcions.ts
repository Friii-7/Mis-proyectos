// (() => {
//   const calcTotal = (prices: number[]) => {
//     let total = 0;
//     prices.forEach((item) => {
//       total += item;
//     });
//   };
// })();

function imprimirNomero(youraName: string): void {
  console.log(`Hello ${youraName}`);
}
